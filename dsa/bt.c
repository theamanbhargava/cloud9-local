#include <stsio.h>
#include <stdlib.h>

struct bstnode
{
    int data;
    struct bstnode* left;
    struct bstnode* right;
};

bstnode* getnewnode(int data)
{
    bstnode* newnode= (strcut bstnode*)malloc(sizeof(struct bstnode))
}

void insertnode(bstnode* rootptr, int data)
{
    
}

int main(void)
{
    struct bstnode* rootptr;
    rootptr = NULL;
    insertnode(root, 15);
    insertnode(root, 10);
    insertnode(root, 20);
    
}